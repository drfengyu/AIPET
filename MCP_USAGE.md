# MCP 使用指南

## 项目特定的 MCP 使用场景

### 1. 文件系统操作

#### 读取项目文件
```
读取 src/renderer/App.tsx 文件内容
```

#### 查找文件
```
查找所有 .tsx 文件
```

#### 写入文件
```
在 src/components 目录下创建新组件
```

### 2. GitHub 操作

#### 查看仓库状态
```
查看当前分支状态
```

#### 创建 Pull Request
```
创建一个 Pull Request 来合并新功能
```

#### 管理 Issues
```
列出所有开放的 Issues
```

### 3. 数据库操作

#### 查询聊天记录
```
查询聊天记录表中的所有数据
```

#### 管理用户数据
```
创建用户表并插入测试数据
```

#### 执行 SQL 查询
```
执行 SQL 查询统计聊天次数
```

### 4. 网络请求

#### 测试 API 连接
```
测试开发服务器是否运行
```

#### 获取外部数据
```
获取 Live2D 模型更新信息
```

## 项目开发工作流

### 1. 代码开发
```
1. 读取现有代码
2. 理解代码结构
3. 修改或添加新功能
4. 测试代码
```

### 2. Live2D 集成
```
1. 读取模型配置文件
2. 修改模型参数
3. 测试模型加载
4. 调试动画效果
```

### 3. AI 代理开发
```
1. 读取代理代码
2. 修改代理逻辑
3. 测试代理功能
4. 集成到项目中
```

### 4. 打包发布
```
1. 检查构建配置
2. 执行构建命令
3. 测试打包结果
4. 发布到 GitHub
```

## 常用命令

### 文件操作
- `读取 <文件路径>` - 读取文件内容
- `写入 <文件路径> <内容>` - 写入文件
- `列出 <目录路径>` - 列出目录内容
- `搜索 <模式>` - 搜索文件

### Git 操作
- `查看状态` - 查看 Git 状态
- `提交更改` - 提交代码更改
- `创建分支` - 创建新分支
- `合并分支` - 合并分支

### 数据库操作
- `查询 <SQL>` - 执行 SQL 查询
- `创建表 <表名>` - 创建新表
- `插入数据` - 插入测试数据

### 网络操作
- `测试连接 <URL>` - 测试网络连接
- `获取数据 <URL>` - 获取外部数据

## 项目特定配置

### 开发服务器
- **地址**: http://localhost:5174/
- **端口**: 5174

### Electron 应用
- **主进程**: src/main/index.ts
- **渲染进程**: src/renderer/index.tsx

### Live2D 模型
- **模型路径**: public/models/live2d-model.json
- **纹理路径**: public/models/textures/

### AI 代理
- **代理路径**: src/agents/
- **类型定义**: src/agents/types.ts

## 故障排除

### MCP 服务器未启动
```
检查 .claude/settings.json 配置
确保 npx 可用
```

### 文件系统权限
```
检查文件读写权限
确保路径正确
```

### 数据库连接失败
```
检查数据库文件路径
确保数据库文件存在
```

### 网络请求失败
```
检查网络连接
确保 URL 正确
```
